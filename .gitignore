# Build artifacts
build/
dist/
__pycache__/
*.pyc

# macOS cruft
.DS_Store

# Icon build intermediates (keep icon.icns / icon.ico)
AppIcon.iconset/
icon_1024.png

# NEVER commit or ship the plaintext keys — only licenses.py (hashes) ships.
LICENSE_KEYS.txt
